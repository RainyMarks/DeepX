{
  "name": "synthetic-image-forensics-atlas",
  "private": true,
  "version": "2.0.0",
  "description": "生成图像取证研究图谱：中文学术地图与本地审核工作台",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "test": "npm --prefix web run test",
    "test:e2e": "npm --prefix web run test:e2e",
    "typecheck": "npm --prefix web run typecheck",
    "data:validate": "python -m atlas.cli validate-public",
    "admin": "python -m atlas.cli admin"
  }
}
